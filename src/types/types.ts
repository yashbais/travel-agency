export type servicesCardTypes = {
  title: string;
  content: string;
  iconImage: string;
};

export type destinationCardTypes = {
  country: string;
  amount: string;
  Image: string;
  Trip : string;
};
